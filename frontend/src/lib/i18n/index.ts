/**
 * i18n (Internationalization) module for GoPayShortcuts
 * Provides localization support for Danish (da) and English (en)
 */

export { locale, translations, t, detectBrowserLanguage, type Locale } from './store';
